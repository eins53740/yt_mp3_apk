---
phase: 02-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - build.gradle.kts
  - app/build.gradle.kts
  - app/proguard-rules.pro
autonomous: true
requirements:
  - MOD-01
must_haves:
  truths:
    - "App builds successfully after all dependency upgrades (zero functional regression)"
    - "Kotlin 2.x Compose compiler plugin replaces the old composeOptions block"
    - "KSP plugin is declared and ready for Hilt code generation in Plan 02"
    - "Coil dependency removed (unused in codebase)"
  artifacts:
    - path: "gradle/libs.versions.toml"
      provides: "Modern dependency versions and new plugin declarations"
      contains: "kotlin = \"2.1.21\""
    - path: "build.gradle.kts"
      provides: "Root-level plugin declarations for compose-compiler, hilt, and ksp"
      contains: "compose.compiler"
    - path: "app/build.gradle.kts"
      provides: "App-level plugin applications and updated dependencies"
      contains: "compose.compiler"
    - path: "app/proguard-rules.pro"
      provides: "Hilt ProGuard keep rules for release builds"
      contains: "HiltViewModel"
  key_links:
    - from: "gradle/libs.versions.toml"
      to: "build.gradle.kts"
      via: "version catalog aliases"
      pattern: "alias\\(libs\\.plugins"
    - from: "app/build.gradle.kts"
      to: "Compose compiler"
      via: "compose-compiler plugin replaces composeOptions block"
      pattern: "plugins.*compose.compiler"
    - from: "gradle/libs.versions.toml"
      to: "KSP annotation processor"
      via: "ksp version must match kotlin prefix"
      pattern: "ksp = \"2.1.21"
---

<objective>
Bump all core dependencies to modern versions and configure the Gradle build for Kotlin 2.x, KSP, and Hilt plugin infrastructure.

Purpose: The codebase runs on Kotlin 1.9.0 and Compose BOM 2023.08.00 — both are over two years old. Kotlin 2.x requires replacing the `composeOptions` block with the `org.jetbrains.kotlin.plugin.compose` Gradle plugin. KSP is needed for Hilt code generation (Plan 02). This plan brings the build system up to date without changing any Kotlin source files — the app behavior is unchanged.

Output: Updated `libs.versions.toml`, both `build.gradle.kts` files, and `proguard-rules.pro`. No Kotlin source files touched.
</objective>

<execution_context>
@C:/Users/bsdias/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/bsdias/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-foundation/02-RESEARCH.md

Source files to modify:
@gradle/libs.versions.toml
@build.gradle.kts
@app/build.gradle.kts
@app/proguard-rules.pro

<interfaces>
<!-- No code interfaces — this plan modifies only Gradle configuration and ProGuard rules. -->
<!-- Gradle wrapper is already at 8.13 (from gradle-wrapper.properties), compatible with AGP 8.13.2 and Kotlin 2.1.21. -->
<!-- Coil is declared in libs.versions.toml and app/build.gradle.kts but is NOT used anywhere in Kotlin source files — remove it entirely. -->
<!-- Phase 1 already set AGP to 8.13.2 — do NOT change AGP version. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update libs.versions.toml with modern versions and new plugin/library declarations</name>
  <files>gradle/libs.versions.toml</files>
  <action>
Replace the entire `gradle/libs.versions.toml` with updated versions and new declarations. Changes from the current file:

**[versions] section changes:**
- `kotlin`: `"1.9.0"` -> `"2.1.21"`
- `composeBom`: `"2023.08.00"` -> `"2026.02.00"`
- `coreKtx`: `"1.12.0"` -> `"1.15.0"`
- `lifecycleRuntimeKtx`: `"2.6.2"` -> `"2.10.0"`
- `activityCompose`: `"1.8.1"` -> `"1.10.0"`
- REMOVE `coil = "2.5.0"` (Coil is unused in source code — no AsyncImage or rememberAsyncImagePainter calls exist)
- ADD `ksp = "2.1.21-2.0.1"` (KSP version prefix MUST match Kotlin version exactly)
- ADD `hilt = "2.57.1"`
- ADD `hiltNavigationCompose = "1.3.0"`
- ADD `lifecycleRuntimeCompose = "2.10.0"` (for `collectAsStateWithLifecycle()` in Plan 03)
- `agp` stays at `"8.13.2"` (DO NOT CHANGE — already correct from Phase 1)
- `youtubedlAndroid` stays at `"0.18.0"` (DO NOT CHANGE)
- Test dependency versions stay the same (DO NOT CHANGE)

**[libraries] section changes:**
- REMOVE `coil-compose` line
- ADD `hilt-android = { group = "com.google.dagger", name = "hilt-android", version.ref = "hilt" }`
- ADD `hilt-android-compiler = { group = "com.google.dagger", name = "hilt-android-compiler", version.ref = "hilt" }`
- ADD `hilt-navigation-compose = { group = "androidx.hilt", name = "hilt-navigation-compose", version.ref = "hiltNavigationCompose" }`
- ADD `lifecycle-runtime-compose = { group = "androidx.lifecycle", name = "lifecycle-runtime-compose", version.ref = "lifecycleRuntimeCompose" }`
- All existing libraries keep their current catalog names/groups

**[plugins] section changes:**
- ADD `compose-compiler = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }`
- ADD `hilt-android = { id = "com.google.dagger.hilt.android", version.ref = "hilt" }`
- ADD `ksp = { id = "com.google.devtools.ksp", version.ref = "ksp" }`
- Existing `android-application` and `jetbrains-kotlin-android` plugins stay as-is

**Why these specific versions:**
- Kotlin 2.1.21: Latest stable 2.1.x. Conservative jump from 1.9.0 (2.3.x is newer but unnecessary risk). Requires AGP 8.6+ (we have 8.13.2).
- KSP 2.1.21-2.0.1: KSP prefix MUST match Kotlin version. If mismatched, build fails with "This version of KSP requires Kotlin X.Y.Z".
- Compose BOM 2026.02.00: Maps to material3 1.4.0, runtime/ui 1.10.3. Managed by BOM — no individual Compose library versions needed.
- Hilt 2.57.1: Latest stable Hilt with KSP support. hilt-android and hilt-android-compiler must be same version.
- lifecycleRuntimeCompose 2.10.0: Provides `collectAsStateWithLifecycle()` needed in Plan 03.
- coreKtx 1.15.0, activityCompose 1.10.0: Latest stable versions compatible with the new BOM.
  </action>
  <verify>
    <automated>grep -c "kotlin = \"2.1.21\"" gradle/libs.versions.toml && grep -c "composeBom = \"2026.02.00\"" gradle/libs.versions.toml && grep -c "ksp = \"2.1.21-2.0.1\"" gradle/libs.versions.toml && grep -c "hilt = \"2.57.1\"" gradle/libs.versions.toml && grep -c "coil" gradle/libs.versions.toml | grep "^0$"</automated>
  </verify>
  <done>
  - libs.versions.toml has Kotlin 2.1.21, Compose BOM 2026.02.00, KSP 2.1.21-2.0.1, Hilt 2.57.1
  - Coil entries are completely removed
  - Hilt, KSP, compose-compiler plugins are declared
  - AGP and youtubedl-android versions are unchanged
  </done>
</task>

<task type="auto">
  <name>Task 2: Update build.gradle.kts files for Kotlin 2.x plugins and remove Coil</name>
  <files>build.gradle.kts, app/build.gradle.kts</files>
  <action>
**Root build.gradle.kts** — Add new plugin declarations with `apply false`:

Current:
```kotlin
plugins {
    alias(libs.plugins.android.application) apply false
    alias(libs.plugins.jetbrains.kotlin.android) apply false
}
```

Replace with:
```kotlin
plugins {
    alias(libs.plugins.android.application) apply false
    alias(libs.plugins.jetbrains.kotlin.android) apply false
    alias(libs.plugins.compose.compiler) apply false
    alias(libs.plugins.hilt.android) apply false
    alias(libs.plugins.ksp) apply false
}
```

**app/build.gradle.kts** — Three changes:

1. **Add plugins** — In the `plugins {}` block, add three new lines after the existing two:
```kotlin
plugins {
    alias(libs.plugins.android.application)
    alias(libs.plugins.jetbrains.kotlin.android)
    alias(libs.plugins.compose.compiler)
    alias(libs.plugins.hilt.android)
    alias(libs.plugins.ksp)
}
```

2. **Remove composeOptions block** — Delete the entire `composeOptions { ... }` block (currently lines 55-57):
```kotlin
// DELETE THIS:
composeOptions {
    kotlinCompilerExtensionVersion = "1.5.1"
}
```
This block is REPLACED by the `compose-compiler` plugin. Leaving it in causes a build warning/error with Kotlin 2.x. Do NOT add a `composeCompiler {}` block in its place — the defaults are correct.

3. **Update dependencies** — In the `dependencies {}` block:
- REMOVE: `implementation(libs.coil.compose)` (Coil is unused — no AsyncImage calls exist in source)
- ADD: `implementation(libs.hilt.android)`
- ADD: `ksp(libs.hilt.android.compiler)` (use `ksp()` NOT `kapt()` — kapt is incompatible with Kotlin 2.x K2 compiler)
- ADD: `implementation(libs.hilt.navigation.compose)`
- ADD: `implementation(libs.lifecycle.runtime.compose)`

Keep ALL existing dependencies unchanged (core-ktx, lifecycle-runtime-ktx, lifecycle-viewmodel-compose, activity-compose, compose-bom, ui, material3, youtubedl, test dependencies).

**DO NOT:**
- Add `kapt` plugin — it is incompatible with K2 compiler
- Change `compileSdk`, `targetSdk`, `minSdk`, `versionCode`, or `versionName`
- Change the `splits` block, `buildTypes`, `compileOptions`, `kotlinOptions`, or `packaging` blocks
- Add a `composeCompiler {}` configuration block — defaults are correct
  </action>
  <verify>
    <automated>grep -c "compose.compiler" app/build.gradle.kts && grep -c "hilt.android" app/build.gradle.kts && grep -c "ksp" app/build.gradle.kts && grep -c "composeOptions" app/build.gradle.kts | grep "^0$" && grep -c "coil" app/build.gradle.kts | grep "^0$"</automated>
  </verify>
  <done>
  - Root build.gradle.kts declares compose-compiler, hilt-android, ksp plugins with `apply false`
  - app/build.gradle.kts applies compose-compiler, hilt-android, ksp plugins
  - composeOptions block is removed from app/build.gradle.kts
  - Coil dependency is removed from app/build.gradle.kts
  - Hilt, KSP dependencies added (ksp() not kapt())
  - lifecycle-runtime-compose and hilt-navigation-compose dependencies added
  - No other configuration blocks changed
  </done>
</task>

<task type="auto">
  <name>Task 3: Add Hilt and data class ProGuard rules for release builds</name>
  <files>app/proguard-rules.pro</files>
  <action>
Add Hilt-specific ProGuard keep rules to `app/proguard-rules.pro`. Append AFTER the existing rules (do NOT modify any existing rules).

Add at the end of the file:
```proguard
# Hilt DI — prevent R8 from stripping @HiltViewModel constructors (Hilt 2.56+ bug)
# See: https://github.com/google/dagger/issues/4739
-keepnames @dagger.hilt.android.lifecycle.HiltViewModel class * extends androidx.lifecycle.ViewModel
-keep class dagger.hilt.** { *; }
-keep class javax.inject.** { *; }
-keep class * extends dagger.hilt.android.internal.managers.ViewComponentManager$FragmentContextWrapper { *; }

# Keep DownloaderUiState data class (created in Plan 03)
-keepclassmembers class com.example.yt2local.DownloaderUiState { *; }
```

**Why:** A known bug in Hilt 2.56+ causes R8 to strip `@HiltViewModel`-annotated constructor parameters in release builds. Without this rule, the release APK crashes on first screen load with `NoSuchMethodException`. This affects debug=false builds only — the rule is harmless in debug builds.

The `DownloaderUiState` rule is added proactively for Plan 03 (StateFlow migration) which will create this data class. Per CLAUDE.md: "Add new data classes to ProGuard rules when creating them."

**DO NOT** modify or remove any existing rules in the file.
  </action>
  <verify>
    <automated>grep -c "HiltViewModel" app/proguard-rules.pro && grep -c "dagger.hilt" app/proguard-rules.pro && grep -c "DownloaderUiState" app/proguard-rules.pro</automated>
  </verify>
  <done>
  - ProGuard file contains Hilt keep rules for @HiltViewModel, dagger.hilt.**, javax.inject.**
  - ProGuard file contains keep rule for DownloaderUiState data class
  - All existing rules are untouched
  </done>
</task>

</tasks>

<verification>
1. `grep 'kotlin = "2.1.21"' gradle/libs.versions.toml` returns a match
2. `grep 'ksp = "2.1.21-2.0.1"' gradle/libs.versions.toml` returns a match
3. `grep 'composeBom = "2026.02.00"' gradle/libs.versions.toml` returns a match
4. `grep 'compose.compiler' app/build.gradle.kts` returns a match
5. `grep 'composeOptions' app/build.gradle.kts` returns NO match (block removed)
6. `grep 'coil' gradle/libs.versions.toml app/build.gradle.kts` returns NO match (removed)
7. `grep 'kapt' app/build.gradle.kts` returns NO match (using ksp, not kapt)
8. `grep 'HiltViewModel' app/proguard-rules.pro` returns a match
9. `grep 'agp = "8.13.2"' gradle/libs.versions.toml` confirms AGP unchanged
10. No Kotlin source files (.kt) were modified
</verification>

<success_criteria>
- All four build configuration files are updated for Kotlin 2.x, Compose BOM 2026, KSP, and Hilt plugin infrastructure
- The composeOptions block is removed and replaced by the compose-compiler plugin
- Coil is removed entirely (vestigial dependency with no source usage)
- ProGuard rules are ready for Hilt @HiltViewModel in release builds
- No Kotlin source files were touched — app behavior is identical to Phase 1
</success_criteria>

<output>
After completion, create `.planning/phases/02-foundation/02-01-SUMMARY.md`
</output>
